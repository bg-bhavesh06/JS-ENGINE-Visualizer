{
  "name": "js-engine-visualizer",
  "version": "1.0.0",
  "description": "Root package for running JS Engine Visualizer",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install-all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "dependencies": {
    "all": "^0.0.0"
  }
}
